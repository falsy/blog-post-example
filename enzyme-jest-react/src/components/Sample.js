import React from 'react';
import { statusPage, pageTitle } from '../styles/sample.scss';

const Sample = () => {
  return (
    <section className={statusPage}>
      <h1 className={pageTitle}>SAMPLE PAGE</h1>
    </section>
  );
};

export default Sample;